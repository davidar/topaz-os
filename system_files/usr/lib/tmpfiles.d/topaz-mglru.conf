# MGLRU anti-thrash (ledger 0027): if reclaim would evict pages the
# workload touched within the last second, OOM-kill the offender instead
# of thrash-locking the desktop. The same policy ChromeOS ships.
w /sys/kernel/mm/lru_gen/min_ttl_ms - - - - 1000
