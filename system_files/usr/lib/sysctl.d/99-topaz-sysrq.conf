# Full SysRq (ledger 0027): Alt+PrtSc+F is a manual, kernel-level OOM
# kill that works even while userspace is frozen — the recovery path
# when memory pressure has locked the desktop.
kernel.sysrq = 1
